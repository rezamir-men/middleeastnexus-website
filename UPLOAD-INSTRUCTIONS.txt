UPLOAD THESE FIVE HTML FILES TO THE ROOT OF THE GITHUB REPOSITORY.

They replace the old redirect-only files.

Purpose:
- The old .html URL will redirect once to the extensionless legacy URL.
- The extensionless legacy URL will return a normal 200 page.
- The page content is the same as the current canonical article.
- rel=canonical points to the current /from-tehran-to-baghdad-part-X URL.
- No JavaScript redirect, meta refresh, or noindex remains.

Do not add these five legacy URLs to sitemap.xml.
Do not change the current canonical article filenames.
